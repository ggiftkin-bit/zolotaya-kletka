# Очередь края · 0.12.x

Дата: 2026-09-07.
Для автора и билд-режима.
Не сборка. Порядок слоёв.

Сдано: пни, хозяин, почта, бой, сверка, канон 0.9.1, вред 0.10.0, рост 0.10.1, ордер 0.11.0, услуга 0.11.1 (`59a0c69`), имя деревни двух почт 0.12.0, доска имени 0.12.1, склад к станку 0.12.2.

Собирать **только текущий** бриф. Соседний не «заодно».

## Закон

- Новое правило — модуль гнезда. Стор только клеит.
- `p2p.ts` не контур. После заливки свой двор снова «you».
- Вред `writeHarmDeed`. Рост `GROW_CATCHUP_TICKS` = 48. Ордер `writeStallDeed`. Услуга `writeServiceDeed`.
- Имя деревни — `pact.ts` + `writeVillageDeed`. Доска — `streetNotices` / `readStreetNotices`. Склад — `pullNeed` в `pile.ts`. `HAMLETS` не двигать. Выборы не открывать «заодно».

## Слои

| № | Срез | Версия | Файл |
|---|---|---|---|
| 1–5 | сданы | 0.9.1–0.11.1 | см. выше |
| 6a | Имя деревни для двух почт | **сдан 0.12.0** | `docs/BRIEF-BUILD-сход-должность.md` |
| 6b | Доска `board` этого имени | **сдан 0.12.1** | `docs/BRIEF-BUILD-доска.md` |
| 6b+ | Склад к станку | **сдан 0.12.2** | `docs/BRIEF-BUILD-склад-станок.md` |
| 6c | Председатель и выборы | позже | — |
| 6d | Казна и доля улицы | позже | — |

Не эта очередь: поход, таран, чат, стрельба башни, p2p, пачка новых рецептов.
