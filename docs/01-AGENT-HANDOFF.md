# Агенту · Золотая Клетка 0.21.3

Живая канвас-игра. Репозиторий: https://github.com/ggiftkin-bit/zolotaya-kletka

Игрок видит только превью. Сервер: `npm run dev` на `0.0.0.0:8080`. После правок — `npx tsc --noEmit` и push в `main`.

**Прямой бриф бьёт этот файл.** Один заход — одна тема. Не чинить реку «заодно» с боем.

Версия: `0.x.y` — ноль всегда (альфа). `x` — новая механика. `y` — доработка того что есть. Сейчас **0.21.3**: гонка клетки — двое рубят, один взял. Тело книги 0.21.2. Потолок сдач 0.21.1. Сила и раны 0.21.0. Хвост мешка 0.20.1. Сумка 0.20.0. Золото 0.19.0. Удар не пересобирать.

---

## Правда стола

Если кнопка делает иначе, чем текст — верна кнопка. Цифры брать из `src/game/pace.ts` и `economy.ts`, не из памяти чата.

Живые якоря кода (менять можно, если бриф просит):

| что | где сейчас |
|---|---|
| ключ кармана | `zolotaya-kletka-v8` в `save.ts` — не поднимать без нужды |
| сид земли | `kletka-land-02` |
| карта | 96×96, `TILE = 44` |
| версия | `GAME_VERSION` в `constants.ts` |
| кружка | 8 · ускорить 12 · руки 16 · залог 20 |
| сила капает | поле 90 с / крыша 45 / сон 20 |
| шаг силы | пешком 0.5 · конь 0.2 · тачка 0.7 · телега 0.4 |
| сытость/тик | крыша −1, поле −2 |
| док | Стоп · Ко мне · Съесть · Сумка |

Река, туман, формула удара — не святыня. Трогать, когда бриф про них.

Пуш: зеркало `/tmp/zk-push`, автор `ggiftkin-bit <ggiftkin@gmail.com>`. После каждой обновы — коммит с номером версии и push.

---

## Карта файлов

| зона | файлы |
|---|---|
| ход, сбор, встреча | `src/game/store.ts` |
| бой | `src/game/fight.ts`, встреча в книге — `book-api.ts` `openBookFight` / `strikeBookFight` |
| генерация | `worldgen.ts` |
| отрисовка | `BoardCanvas.tsx` |
| пузырь / шапка | `TileBubble.tsx`, `Hud.tsx` |
| книга мира | `book.ts`, `book-api.ts`, `book-sync.ts` — тело фишки держит `busy` и `travel` |
| вход | почта: `EmailGate.tsx`, флаг `src/lib/auth/email-password.ts` |
| числа | `constants.ts`, `economy.ts`, `pace.ts` |
| сход | `pact.ts` — имя на клетках; доска — знак на тракте/берегу/поляне/улице, не мебель двора; хутора `HAMLETS` не двигать |
| рынок | `market.ts` — ордер, услуга у калитки/станка/плота. Доска — лист хозяина столба, заказ на цель |
| куча | `pile.ts` — склад кормит станок |
| ремесло | `craft.ts` — мука дома или у печи, хлеб из муки, кирпич у печи |
| тын | `fence.ts` — двор без калитки не замкнуть; снял забор — дом на месте, столба-заплаты нет |
| контора | `office.ts` — склад тракта 0/100, призы, донат. Книга держит `world.stock` и `pawn.body.gifts`. Сдачи лавке: 12 за сутки книги, счёт `sells`/`sellDay` |
| золото | `gold.ts` + `book-api.ts` `writeGoldDeed`. Upsert фишки не берёт `gold`/`due`/`gifts` с клиента |
| сумка | `bag.ts` + `book-api.ts` `writeBagDeed`. Upsert не берёт `inventory` с клиента. Стол шлёт род и клетку. Рубка/куча/сундук: `ver` не сошёлся или запас ноль — «уже нет», сумка не растёт |
| сила | `pace.ts` + `writeGoldDeed` / `writeBagDeed`. Upsert не берёт `energy`/`hp`/`life`/`deaths`. Реген на сердцебиении, кружка +4, сон — род `sleep` |
| тело | `pace.ts` `tickFlesh` + `writeBagDeed` eat/drink/pail/sip/cook. Upsert не берёт `satiety`/`warmth`/`water`/`pail`. Тик мира — часы книги |
| стоянка | `mount.ts` — тачка и лошадь на клетке, как телега. Увод — `crime.ts` `planDriveOff` |

Документы: этот файл → `docs/СПРАВОЧНИК.md` → `05-WORLD-BOOK.md` → `public/game/TEXTURE-MAP.md`.

---

## Вход и аккаунт

Почта и пароль. Google и X на столе нет. С другого устройства — тот же `user_id`, та же фишка, та же земля.

Клетка общая. Сумка и тело — в `pawn.body`. Двор в книге пишется `user_id`, на экране свой двор это «you».

Карман `v8` — черновик. Хозяин клеток — книга. Ход и дело в `pawn.body`: закрыл стол — часы всё равно идут, откроешь — фишка догнала. Золото, сумка, сила, раны, сытость, тепло и вода тела — только книга: стол шлёт род дела, не цифру.

Вред (куча, засов, кража, поджог) пишет клетку и фишку в книгу сразу, с `ver`. Закон двора смотрит клетки в книге, манекен не обязателен. Взлом и поджог — дело, не мгновенный тык. Бой живых по-прежнему полка `fight`.

Рост: часы `world.clock_at`, правило `grow.ts`, пишет сервер книги. Клиент при открытой книге календарь не крутит. Догонялка после сна — `GROW_CATCHUP_TICKS` = 48 (неделя). Зиму за кадр не крутить.

Прилавок: ордер в slim клетки (`or`). Вещь сразу на витрине. Берёт другая почта, стоя Чебышёв ≤1 в живом пятне. Золото капает продавцу в `pawn.body.due`, даже если стол закрыт. Лавка тракта: сырьё пачкой со склада книги (`world.stock`), не с квоты недели.

Услуга: контракт в slim клетки (`sv`). Постой, отвези, привези, построй, сделай. Вешают у калитки, прилавка, станка и пустого плота. На клетку доски услугу не вешают — там лист улицы. Привези: золото в эскро, вещи нет. Исполнитель несёт своё на цель. Золото и сырьё отвези/станка в эскро с первого тыка. Вывеска без часа. Взяли — срок на дело. Вторая почта берёт в пятне, одно `Busy`. Не выполнил — золото заказчику. Не витрина ордера и не руки за 16.

Имя деревни: `village` на клетках в книге. Старый сход — ты + 4 друга хуторов. Новый — два живых двора, края Чебышёв ≤2, вторая почта принимает в пятне. Уйти — только свои клетки. Доску ставят на тракте, берегу, поляне или улице имени. Плот под доску не берётся. Лист — заказы хозяина столба; имя улицы — плюс строки этого имени. Без имени лист не пуст, если висят заказы. На клетку доски услугу не вешают. Склад в двух клетках отдаёт сырьё на дело. Кирпич — у печи, любой. Поле даёт зерно. Мука — 2 зерна, дом или печь, любой. Хлеб — 2 муки, пекарь, печь. Выборы и казна — не этот слой.

Склад тракта: куча в `world.stock` по товару. Старт 40. Выше 100 — лавка у игрока не берёт («склад полон»). Ноль — игроку не продаёт («нет на складе»). Золото сдачи из бесконечного мешка игры. Золото покупки в никуда. Контора — дверь той же клетки: три приза не в сумке, донат +50 на эту почту.

Тачка и лошадь — на клетке, не в сумке. `tile.cart` / `tile.horse` = хозяин. Взял — `transport`, клетка пустая. Оставил — снова здесь. Упал / яма — сразу на этой клетке. Чужую стоящую (тачка, лошадь, телега) уводят делом «увести», как кучу и засов. Своё — без дела. Без закона — стала твоя. Двор с законом: срыв — след, довёл — увёл.

---

## Сверка

Сердцебиение ~800 мс. Ход клетки сразу шлёт фишку. Не ждать заливку, чтобы увидеть чужого.

---

## Бой живых

Только на одной клетке. Манекен — как был, локально.

Живой: «Встретиться» пишет встречу в книгу. У кого напали — лист сам: «Напали». Удар идёт в книгу, здоровье пишет сервер, ход меняется. Пока не твой шаг — «Ждёт ответа».

---

## Как править

1. Бриф. Один слой.
2. Цифры из кода.
3. `npx tsc --noEmit`.
4. Пуш с пометкой версии.
