# Бриф · иконки

Пляшем от 0.27.2
Версия сдачи: **0.27.3** — один раз, когда готовы A–B.

Две очереди. Сид и карман не поднимать. Закон книги не трогать.

Листы уже нарисованы. Не генерировать заново. Взять файлы из этого брифа / чата разработчика и положить в `public/game/`.

---

## Почему

`extras.png[3]` — пенёк, а код берёт его как лопату.
Шкура — морковка. Жилет — верёвка. Клобук — руда. Удочка — рыба.
На клетке стадо `28×28` при `TILE 44` — мелко.

---

## A · листы и клей

Положить (заменить только эти):

| файл | сетка | что |
|---|---|---|
| `public/game/extras.png` | 2×2 | трава, глина, кристалл, **лопата** |
| `public/game/life.png` | 3×3 | те же индексы, звери крупнее в ячейке |
| `public/game/goods.png` | 4×4 | новый лист ремесла |
| `public/game/armor.png` | 2×2 | стёганка, шлем, щит, кирпич |

`icons.png`, `gear.png`, `jobs.png`, `props.png`, `tiles.png` — не трогать.

### `goods.png` 4×4 · индекс слева направо сверху вниз

| i | вещь |
|---|---|
| 0 | кирка |
| 1 | удочка |
| 2 | дубина |
| 3 | нож |
| 4 | замок |
| 5 | колесо |
| 6 | доска / планка |
| 7 | слиток |
| 8 | хлеб |
| 9 | копчёное |
| 10 | уголь |
| 11 | настой |
| 12 | шкура |
| 13 | кожа |
| 14 | жилет |
| 15 | клобук |

### `armor.png` 2×2

| i | вещь |
|---|---|
| 0 | стёганка |
| 1 | шлем |
| 2 | щит (тес и кованый одна картинка) |
| 3 | кирпич |

### `ItemPic` в `Sprite.tsx`

Не ломать `Ico` / `icons.png` для золота, дерева, камня, еды-морковки, рыбы, руды, топора, сумки.
`gear.png` — только верёвка, ведро, копьё, капля.

Новое:
- `herb` / `clay` / `sand` / `crystal` / `shovel` / `steel_shovel` — `extras.png` как сейчас (`sand` остаётся глиной)
- `pick` / `steel_pick` → goods 0
- `rod` → goods 1, убрать отдельный `rod.png` если был
- `club` → goods 2
- `knife` → goods 3
- `lock` → goods 4
- `wheel` → goods 5
- `plank` → goods 6
- `bar` → goods 7
- `bread` → goods 8
- `smoked` → goods 9
- `coal` → goods 10
- `tonic` → goods 11
- `hide` → goods 12
- `leather` → goods 13
- `vest` → goods 14
- `hood` → goods 15
- `wadded` → armor 0
- `helm` → armor 1
- `board_shield` / `bar_shield` → armor 2
- `brick` → armor 3
- `grain` / `flour` — по-прежнему еда из `icons.png`
- `steel_axe` — топор из `icons.png`

Добавить `GoodsIco` и `ArmorIco` рядом с `ExtraIco` / `GearPic`. Тот же `Sprite`.

Приёмка A:
1. Сумка → лопата выглядит как лопата.
2. Шкура — шкура, кожа — рулон, жилет — жилет, клобук — капюшон.
3. Удочка — удочка, не рыба. Настой — склянка, не трава.
4. Верёвка и топор как были.

---

## B · звери на клетке

`BoardCanvas.tsx` стадо и лошадь сейчас ~26–28 px.

Закон:
- заяц / олень / волк / корова / лошадь на клетке: **40×40**, чуть смещены к центру клетки
- загон / конюшня / колодец с `life.png` — как были, на всю клетку
- индексы `LIFE_INDEX` не менять

Приёмка B:
1. Заяц на лугу крупнее старого, не закрывает соседнюю клетку.
2. Две коровы в загоне читаются.
3. Шалаш, рынок, ход — не сломали.

---

## Не делать

Новые станки, выборы, казна, ноша, v8, 192, новый сид.
Не перерисовывать `icons` / `jobs` / `props` в этом слое.
`store.ts` не трогать.

---

## Сдача

После A — коротко. Версию не поднимать.
После B — полный лист / файлы / **0.27.3** / `tsc` / sha.
