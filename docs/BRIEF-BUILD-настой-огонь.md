# Бриф · настой, разбор и огонь

**Срез сдан.** 0.25.1

Пляшем от 0.25.0
Версия сдачи: 0.25.1

Один слой. Кожу не пересобирать.
Цифры цены постройки — из `economy.ts` / `constants.ts`, не из чата.

---

## A · настой пишет книга

Сейчас `drinkTonic` красит hp на столе и шлёт в книгу только трату вещи. Upsert не берёт `hp` с клиента. Пятно вернуло раны.

Закон:
- род дела `tonic` (или расширить `writeBagDeed` / `pace.ts` так, чтобы раны считала книга)
- снял 1 настой, `hp +28`, потолок 100
- цел (100) — настой не пьёт, как сейчас
- экран не хозяин ран. Пятно не откатывает выпитое

---

## B · уголь после огня

Сгоревший шалаш — не куча на клетке. «Поднять» смотрит кучу. Кучи нет — в сумку ничего не класть и не глотать вещь в никуда.

`scrapBurned` сейчас местно даёт дерево и часто без печати сумки.

Закон:
- сгоревший остов разбирают делом «разобрать», не «поднять кучу»
- в сумку идёт **уголь**, не дерево: шалаш / костёр 1, дом и прочее дерево 2. Камень не горел — угля 0, остов просто снять
- сумка через `bag.ts` + `writeBagDeed`. Места нет — уголь кучей на клетке
- второй тык — «уже нет». `ver` клетки
- здание с клетки уходит

---

## C · разбор живого

Свой шалаш или дом без огня. Кнопка «Разобрать» на листе клетки.

- только своё. Чужое с законом — не разбор, это вред
- назад **половина** цены постройки из `BUILD_COST`, вниз. Шалаш сейчас 6 дерева → 3. Дом 14 дерева + 6 камня → 7 и 3. Если цифра в коде другая — половина от живой
- сундук на землю кучей, не в никуда
- здание с клетки уходит, двор и тын не снимать этим делом
- сумка через книгу. Места нет — куча на клетке
- сгоревший — не этот путь, это B

---

## D · трут и калитка

Поджог уже дело ~8 с. Камень не горит. Калитка уже не в `burnableFence` — так и оставить.

Закон:
- жечь можно, только если в **руке трава**. Нет — «нечем зажечь»
- дело съедает 1 траву из руки / сумки через книгу. Пальцем нельзя
- костёр на соседней клетке не заменяет трут
- калитку не жечь. Засов жив. Чужому с засовом — сначала «Взломать»
- деревянный тын и частокол горят как сейчас. Каменная стена нет
- чужой двор с законом: жребий ямы как сейчас. Без закона — розыск. Свой костёр свою яму не кормит
- яму не пересобирать. Это не капкан на пороге: сажает дело вреда, не шаг во двор

---

## Куда смотреть

Настой — `pace.ts` + `bag.ts` + `writeBagDeed`. Не красить hp только в `store.ts`.
Огонь — `crime.ts` / `work.ts` / `fence.ts`. `store.ts` клеит.
Разбор — новый род сумки, не раздувать `store.ts`.
Куча — `pile.ts`.

---

## Не делать

192, комбинат, выборы, админка, Stars, p2p, NPC.
Кожевня, слот ног, новый удар, новый сид, подъём v8.
Новый торг. Новый предмет «факел». Жечь калитку. Пересборка ямы.
`store.ts` только клеит.

---

## Приёмка

1. Раны 40, выпил настой — здоровье 68. Обновил книгу / перешёл пятно — 68 на месте. Настой съеден.
2. Цел — настой не пьётся.
3. Сжёг шалаш, «разобрать» — уголь в сумке. Второй тык — «уже нет». «Поднять» на пустой куче не глотает мешок.
4. Свой целый шалаш «разобрать» — половина дерева в сумке или кучей, крыши нет. Тын на месте.
5. Без травы в руке — «нечем зажечь». С травой — поджёг съел траву, шалаш сгорел.
6. Калитка с засовом не горит. Взлом жив.
7. Охотник, кожа, жилет, клобук, мельница, падение под крышей живы.
8. На экране 0.25.1. `npx tsc --noEmit` чистый.
